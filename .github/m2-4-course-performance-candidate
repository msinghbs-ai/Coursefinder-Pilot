# CF-239 M2.4.5 targeted performance recovery — 7 September 2026

Source head before marker:
7b4baadbc8ec1d700739015bad1e6b8b8d284981

Trigger:
- CF-238 bounded integration run 34070394953 failed desktop.
- retained Playwright evidence showed evidence_page 3544 ms > 3000 ms, PostgreSQL 57014 statement-timeout 500s under concurrent admin reads, and stale Layer 2 sources test terminology.

Correction:
- default unfiltered Evidence browse now pages first, then enriches only returned rows;
- filtered/non-default Evidence requests retain the existing governed implementation;
- added supporting captured/created/status indexes for existing health/status projections;
- CF-089 Administration helper now targets the governed Extraction Profiles label while retaining the layer2-sources route/backend contract;
- direct Pilot measurements after migration: evidence_page ~193 ms, layer_status_summary ~537 ms, dashboard ~216 ms, courses_page ~65 ms, platform_health ~21 ms.

Focused scope:
- tests/uat/performance-deployed.spec.mjs only.

Hard gates unchanged:
- RPC/detail latency <= 3,000 ms;
- page/management payload <= 250,000 bytes;
- filter payload <= 60,000 bytes;
- zero unexpected HTTP 5xx responses.

This is targeted recovery proof only, not bounded integration or final acceptance.
