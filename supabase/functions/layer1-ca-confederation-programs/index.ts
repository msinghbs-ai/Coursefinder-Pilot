import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import {createClient} from "npm:@supabase/supabase-js@2";
const V="layer1-ca-confederation-programs-v0.1.0",DLI="O19376986752",SYS="ca_on_confederation_programs",SCHEME="confederation_program_id",SOURCE="https://www.confederationcollege.ca/api/program-search.json";
const out=(x:any,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{"content-type":"application/json"}}),t=(x:any)=>String(x??"").trim();
async function rpc(c:any,n:string,a:any={}){const{data,error}=await c.rpc(n,a);if(error)throw new Error(`${n}: ${error.message}`);return data;}
async function sha(b:Uint8Array){return[...new Uint8Array(await crypto.subtle.digest("SHA-256",b))].map(x=>x.toString(16).padStart(2,"0")).join("");}
Deno.serve(async req=>{const url=Deno.env.get("SUPABASE_URL")!,key=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,c=createClient(url,key,{auth:{persistSession:false}});let job:any=null;try{if(req.method!=="POST")return out({error:"POST required"},405);const nonce=t(req.headers.get("x-cf-run-nonce"));if(!nonce||!await rpc(c,"svc_pilot_consume_nonce",{p_function:"layer1-ca-confederation-programs",p_nonce:nonce}))return out({error:"valid one-time Pilot nonce required"},401);const body=await req.json().catch(()=>({})),apply=body.apply===true;const pr=await rpc(c,"svc_layer1_resolve_provider_by_identifier",{p_country_code:"CA",p_scheme:"ircc_dli",p_identifier:DLI}),ps=await rpc(c,"svc_layer1_resolve_provider_sources",{p_provider_id:pr.provider_id}),src=(ps||[]).find((x:any)=>x.system_code===SYS),regs=await rpc(c,"svc_layer1_resolve_sources",{p_country_code:"CA"}),ircc=(regs||[]).find((x:any)=>x.system_code==="ca_ircc_dli");if(!src||!ircc)throw new Error("required source missing");job=await rpc(c,"svc_layer1_start_job",{p_country_code:"CA",p_source_id:src.source_id,p_payload:{provider:"Confederation College",apply,version:V,runtime:"supabase_edge_json"}});const fr=await fetch(SOURCE,{headers:{"user-agent":"CourseFinder-Pilot/Confederation-0.1"}});if(!fr.ok)throw new Error(`Confederation HTTP ${fr.status}`);const text=await fr.text(),bytes=new TextEncoder().encode(text),h=await sha(bytes),doc=JSON.parse(text),allowed=new Set(["degree","diploma","certificate"]),rows:any[]=[],seen=new Set<string>();for(const p of doc.programs??[]){const id=t(p.id),title=t(p.title),cat=t(p.credential?.category),intakes=Array.isArray(p.intakes)?p.intakes:[];if(!id||!title||!allowed.has(cat)||!intakes.some((i:any)=>i.studyMode==="Full-Time"))continue;if(seen.has(id))throw new Error(`duplicate Confederation programme id ${id}`);seen.add(id);rows.push({provider_code:DLI,provider_name:"Confederation College",local_program_id:id,course_title:title,issuing_authority:"Confederation College",lifecycle_status:"active"});}if(rows.length!==59)throw new Error(`Confederation expected 59 full-time college credentials got ${rows.length}`);rows.sort((a,b)=>a.local_program_id.localeCompare(b.local_program_id));const path=`regulatory/CA/ON/confederation/${new Date().toISOString().replace(/[:.]/g,"-")}.json`,up=await c.storage.from("evidence").upload(path,bytes,{contentType:"application/json"});if(up.error)throw new Error(up.error.message);const ev=await rpc(c,"svc_layer1_record_evidence",{p_source_id:src.source_id,p_job_id:job,p_source_url:SOURCE,p_storage_path:path,p_content_hash:h,p_mime_type:"application/json",p_metadata:{worker_version:V,parsed_records:rows.length,identity_scheme:SCHEME,generated_at:doc.generatedAt??null,credential_filter:["degree","diploma","certificate"],study_mode:"Full-Time"}});let rec:any={records:rows.length,course_created:0,course_existing:0,conflicts:0};if(apply)rec=await rpc(c,"svc_layer1_apply_scoped_course_records",{p_country_code:"CA",p_provider_source_id:ircc.source_id,p_course_source_id:src.source_id,p_evidence_id:ev,p_provider_scheme:"ircc_dli",p_course_scheme:SCHEME,p_records:rows});const result={workerVersion:V,mode:apply?"apply":"dry-run",parsedRecords:rows.length,lifecycle:{active:rows.length,inactive:0,suspended:0,unknown:0},evidenceHash:h,reconciliation:rec};await rpc(c,"svc_layer1_finish_job",{p_job_id:job,p_status:"completed",p_result:result,p_error:null});await rpc(c,"svc_layer1_source_health",{p_source_id:src.source_id,p_success:true,p_error:null,p_metadata:{worker_version:V,parsed_records:rows.length,lifecycle:{active:rows.length,inactive:0,suspended:0,unknown:0},evidence_hash:h,last_run_mode:result.mode}});return out({ok:true,jobId:job,...result});}catch(e){const z=e instanceof Error?e.message:String(e);if(job)try{await rpc(c,"svc_layer1_finish_job",{p_job_id:job,p_status:"failed",p_result:{workerVersion:V},p_error:z})}catch{};return out({error:z,jobId:job,workerVersion:V},500);}});