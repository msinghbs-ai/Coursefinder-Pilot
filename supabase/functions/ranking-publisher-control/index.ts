import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const ORIGIN="https://coursefinder-pilot.techm.workers.dev";const LOCAL=new Set(["http://localhost:5173","http://127.0.0.1:5173"]);
const json=(req:Request,status:number,body:unknown)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store","access-control-allow-origin":LOCAL.has(req.headers.get("origin")||"")?(req.headers.get("origin")||ORIGIN):ORIGIN,"access-control-allow-headers":"authorization, x-client-info, apikey, content-type","access-control-allow-methods":"POST, OPTIONS","vary":"origin"}});const clean=(v:unknown)=>String(v??"").trim();const errText=(e:unknown)=>{if(e instanceof Error)return e.message;if(typeof e==="string")return e;try{const s=JSON.stringify(e);return s&&s!=="{}"?s:"unknown error"}catch{return"unknown error"}};
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:{"access-control-allow-origin":ORIGIN,"access-control-allow-headers":"authorization, x-client-info, apikey, content-type","access-control-allow-methods":"POST, OPTIONS"}});if(req.method!=="POST")return json(req,405,{error:"method_not_allowed"});const auth=req.headers.get("authorization")||"";if(!auth.toLowerCase().startsWith("bearer "))return json(req,401,{error:"authentication_required"});const url=Deno.env.get("SUPABASE_URL"),anon=Deno.env.get("SUPABASE_ANON_KEY"),serviceKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!anon||!serviceKey)return json(req,500,{error:"service_configuration_error"});const user=createClient(url,anon,{global:{headers:{Authorization:auth}},auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}}),{data:ctx,error:ctxErr}=await user.rpc("admin_read",{p_operation:"context",p_args:{}});if(ctxErr||!ctx?.authenticated)return json(req,401,{error:"authentication_required"});if(Number(ctx.role_rank||0)<4)return json(req,403,{error:"pipeline_operator_role_required"});const actor=clean(ctx.user_id),body=await req.json().catch(()=>({})),action=clean(body.action).toLowerCase(),importId=clean(body.import_id);if(!["validate","apply"].includes(action))return json(req,400,{error:"unsupported_action"});if(!/^[0-9a-f-]{36}$/i.test(importId))return json(req,400,{error:"valid_import_id_required"});const svc=createClient(url,serviceKey,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}}),{data:imp,error:impErr}=await svc.rpc("svc_ranking_import_control_context",{p_import_id:importId});if(impErr)return json(req,500,{error:"ranking_import_lookup_failed",detail:errText(impErr)});if(!imp?.id)return json(req,404,{error:"ranking_import_not_found"});const systemCode=clean(imp.system_code),sourceId=clean(imp.source_id),filename=clean(imp.original_filename),legacyThe=systemCode==="the_wur"&&!/\.xlsx$/i.test(filename),worker=systemCode==="qs_wur"?"ranking-qs-official-etl":systemCode==="the_wur"&&!legacyThe?"ranking-the-official-etl":"ranking-layer1-etl",jobType="layer1_ranking_etl",{data:jobId,error:jobErr}=await svc.rpc("svc_ranking_job_start",{p_job_type:jobType,p_source_id:sourceId||null,p_requested_by:actor,p_payload:{layer:"layer1",classification:"Layer 1 Ranking ETL",import_id:imp.id,system_code:systemCode,edition_year:imp.edition_year,original_filename:imp.original_filename,worker,legacy_the_evidence:legacyThe,action:action==="validate"?"validate_apply":"apply"}});if(jobErr)return json(req,500,{error:"ranking_job_create_failed",detail:errText(jobErr)});try{const invoke=async(mode:string)=>{const res=await fetch(url+"/functions/v1/"+worker,{method:"POST",headers:{"authorization":"Bearer "+serviceKey,"apikey":serviceKey,"x-cf-layer1-service-key":serviceKey,"content-type":"application/json"},body:JSON.stringify({system_code:systemCode,edition_year:Number(imp.edition_year),mode,source_id:sourceId||"",import_id:importId})}),out=await res.json().catch(()=>({}));if(!res.ok||out?.ok===false||out?.error)throw new Error(out?.error||(worker+" HTTP "+res.status));return out};const out=await invoke(action==="apply"?"apply":"dry_run");let finalOut:any=out,finalAction=action;if(action==="validate"){const validationSummary={candidate_observations:out.candidateObservations,unknown_rank_semantics:out.unknownRankSemantics,indicator_cells:out.indicatorCells,reconciliation_preview:out.reconciliationPreview,worker_version:out.workerVersion,validated_at:new Date().toISOString()};const parseSummary={candidate_observations:out.candidateObservations,filename:out.filename,acquisition_mode:out.acquisitionMode};const{error:updateErr}=await svc.rpc("svc_ranking_import_validation_update",{p_import_id:importId,p_status:"validated",p_validation_summary:validationSummary,p_parse_summary:parseSummary});if(updateErr)throw new Error(errText(updateErr)||"ranking import validation update failed");const applied=await invoke("apply");const appliedSummary={...parseSummary,rows:applied.candidateObservations,mapped:applied.reconciliation?.mapped??applied.partialMapped??null,global_unmapped:applied.reconciliation?.global_unmapped??applied.partialUnmapped??null,supported_scope_unmapped:applied.reconciliation?.supported_scope_unmapped??null,finalized_at:new Date().toISOString(),worker_version:applied.workerVersion};const{error:markErr}=await svc.rpc("svc_ranking_manual_import_mark_applied",{p_import_id:importId,p_validation_summary:validationSummary,p_parse_summary:appliedSummary,p_supersede_inline:true});if(markErr)throw new Error(errText(markErr)||"ranking import applied-state update failed");finalOut={ok:true,mode:"validate_apply",validation:out,apply:applied,reconciliation:applied.reconciliation,workerVersion:applied.workerVersion};finalAction="validate_apply"}else{const{error:markErr}=await svc.rpc("svc_ranking_manual_import_mark_applied",{p_import_id:importId,p_validation_summary:imp.validation_summary||{},p_parse_summary:{...(imp.parse_summary||{}),rows:out.candidateObservations,mapped:out.reconciliation?.mapped??out.partialMapped??null,global_unmapped:out.reconciliation?.global_unmapped??out.partialUnmapped??null,supported_scope_unmapped:out.reconciliation?.supported_scope_unmapped??null,finalized_at:new Date().toISOString(),worker_version:out.workerVersion},p_supersede_inline:true});if(markErr)throw new Error(errText(markErr)||"ranking import applied-state update failed")}const{error:finishErr}=await svc.rpc("svc_ranking_job_finish",{p_job_id:jobId,p_status:"completed",p_result:finalOut,p_error_text:null});if(finishErr)throw new Error(errText(finishErr)||"ranking job completion failed");return json(req,200,{ok:true,action:finalAction,job_id:jobId,import_id:importId,system_code:systemCode,edition_year:imp.edition_year,worker,result:finalOut})}catch(e){const message=errText(e);await svc.rpc("svc_ranking_job_finish",{p_job_id:jobId,p_status:"failed",p_result:{},p_error_text:message});if(action==="validate")await svc.rpc("svc_ranking_import_validation_update",{p_import_id:importId,p_status:imp.status||"uploaded",p_validation_summary:{error:message,failed_at:new Date().toISOString()},p_parse_summary:{}});return json(req,422,{ok:false,error:message,job_id:jobId,import_id:importId,worker})}});
