import React from 'react'
import {BookOpen,ExternalLink} from 'lucide-react'

const empty=v=>v==null||v===''||(Array.isArray(v)&&v.length===0)||(typeof v==='object'&&!Array.isArray(v)&&Object.keys(v||{}).length===0)
const money=x=>x?.amount==null?'—':`${x.currency||x.currency_code||'AUD'} ${Number(x.amount).toLocaleString()}`
const fmtDate=v=>{const d=new Date(v);return Number.isNaN(+d)?String(v||'—'):d.toLocaleDateString(undefined,{day:'2-digit',month:'short',year:'numeric'})}
const bool=v=>v===true?'Yes':v===false?'No':'—'
const evidenceIdOf=v=>v?.evidence_id||v?.source_evidence_id||(v?.storage_path&&v?.id?v.id:null)
const stateMap=rows=>new Map((rows||[]).map(x=>[x.code,x]))

function LayerBadge({layer,struck=false}){const n=Number(layer);if(!n)return null;const label=n===1?'L1 Regulatory':n===2?'L2 Enrichment':n===3?'L3 AI interpreted':'L4 Human resolved';return <span title={label} style={{display:'inline-flex',alignItems:'center',padding:'2px 6px',borderRadius:999,border:'1px solid #cbd5e1',background:'#f8fafc',fontSize:9,fontWeight:800,color:'#475569',whiteSpace:'nowrap',textDecoration:struck?'line-through':'none',opacity:struck?0.6:1}}>L{n}</span>}
function StateTrail({state}){const s=state?.value_state;if(!s)return null;if(s==='resolved')return <span style={{display:'inline-flex',alignItems:'center',gap:4}}><LayerBadge layer={state?.resolved_layer}/><small style={{color:'#64748b'}}>Resolved</small></span>;if(s==='source_missing')return <span style={{display:'inline-flex',alignItems:'center',gap:5}}><LayerBadge layer={1} struck/><small style={{color:'#b45309',fontWeight:700}}>Regulatory correction</small></span>;if(s==='awaiting_l2')return <span style={{display:'inline-flex',alignItems:'center',gap:5}}><LayerBadge layer={2}/><small style={{color:'#64748b',fontWeight:700}}>Awaiting L2</small></span>;if(s==='l2_unresolved'||s==='awaiting_l3')return <span aria-label="Layer 2 attempted and unresolved; awaiting Layer 3" style={{display:'inline-flex',alignItems:'center',gap:5}}><LayerBadge layer={2} struck/><span>→</span><LayerBadge layer={3}/><small style={{color:'#7c3aed',fontWeight:800}}>Awaiting L3</small></span>;if(s==='awaiting_l4')return <span aria-label="Layers 2 and 3 exhausted; awaiting Layer 4 input" style={{display:'inline-flex',alignItems:'center',gap:5}}><LayerBadge layer={2} struck/><LayerBadge layer={3} struck/><span>→</span><LayerBadge layer={4}/><small style={{color:'#b45309',fontWeight:800}}>L4 input</small></span>;if(s==='l4_input')return <span aria-label="Direct Layer 4 input" style={{display:'inline-flex',alignItems:'center',gap:5}}><LayerBadge layer={4}/><small style={{color:'#b45309',fontWeight:800}}>L4 input</small></span>;return null}
function EvidenceButton({id,navigate,courseId,label='Evidence'}){if(!id)return null;return <button className="m-secondary compact" style={{marginLeft:6,padding:'4px 7px',fontSize:9}} onClick={e=>{e.stopPropagation();navigate?.('Evidence',{evidence_id:id,return_course_id:courseId||''})}}><BookOpen size={11}/>{label}</button>}
function Row({label,value,children,layer,state}){return <div><span style={{display:'flex',alignItems:'center',gap:6,flexWrap:'wrap'}}>{label}{state?<StateTrail state={state}/>:<LayerBadge layer={layer}/>}</span><strong>{children??(empty(value)?'—':value)}</strong></div>}
function Section({title,children,help,layer}){return <section className="m-detail-section"><div style={{display:'flex',alignItems:'center',gap:7}}><h3 style={{margin:0}}>{title}</h3><LayerBadge layer={layer}/></div>{help&&<p className="m-help">{help}</p>}{children}</section>}

function Overview({data,states}){const duration=data.duration_value?`${data.duration_value} ${data.duration_unit||''}`.trim():null;return <><div className="m-detail-grid"><div><small style={{display:'flex',gap:5}}>Provider <LayerBadge layer={1}/></small><strong>{data.provider_name||'—'}</strong></div><div><small style={{display:'flex',gap:5}}>CRICOS / Course code <LayerBadge layer={1}/></small><strong>{data.course_code||'—'}</strong></div><div><small style={{display:'flex',gap:5}}>Study level <LayerBadge layer={1}/></small><strong>{data.level_name||data.level_code||'—'}</strong></div><div><small style={{display:'flex',gap:5}}>Field of study <LayerBadge layer={1}/></small><strong>{data.field_name||data.field_code||'—'}</strong></div><Row label="Duration" value={duration} state={states.get('duration')}/><Row label="Delivery" value={data.delivery_mode} state={states.get('delivery_mode')}/><div><small>Lifecycle</small><strong>{data.lifecycle_status||'—'}</strong></div><div><small>Publication</small><strong>{data.publication_status||'—'}</strong></div><div><small>Last verified</small><strong>{data.last_verified_at?fmtDate(data.last_verified_at):'—'}</strong></div><div><small style={{display:'flex',alignItems:'center',gap:5,flexWrap:'wrap'}}>Official Course URL <StateTrail state={states.get('official_course_url')}/></small><strong>{data.course_url?<a href={data.course_url} target="_blank" rel="noreferrer" style={{display:'inline-flex',alignItems:'center',gap:4,color:'#4f46e5',wordBreak:'break-word'}}>Open first-party page <ExternalLink size={11}/></a>:'—'}</strong></div></div><Section title="Course description"><div style={{display:'flex',alignItems:'center',gap:6,marginBottom:6}}><StateTrail state={states.get('course_description')}/></div><p style={{margin:0,lineHeight:1.55}}>{data.description||'—'}</p></Section></>}
function FeeRecord({x,registered,navigate,courseId}){const type=String(x.fee_type||x.type||'tuition').toLowerCase();const title=registered?type==='tuition'?'Registered tuition':type==='non_tuition'?'Registered non-tuition':type==='estimated_total_course_cost'?'Estimated total course cost':'Registered course cost':'Current provider tuition';return <div className="m-record" style={{display:'grid',gridTemplateColumns:'1fr auto',gap:'4px 10px',alignItems:'center'}}><div style={{minWidth:0}}><strong>{title}</strong><div style={{fontSize:17,fontWeight:800,color:'#0f172a',marginTop:4}}>{money(x)}</div></div><LayerBadge layer={registered?1:2}/><div style={{gridColumn:'1 / -1',display:'flex',gap:10,flexWrap:'wrap',fontSize:10,color:'#64748b'}}>{x.fee_year&&<span><b>Year:</b> {x.fee_year}</span>}{x.audience&&<span><b>Audience:</b> {String(x.audience).replaceAll('_',' ')}</span>}{x.basis&&<span><b>Basis:</b> {String(x.basis).replaceAll('_',' ')}</span>}</div><div style={{gridColumn:'1 / -1'}}><EvidenceButton id={evidenceIdOf(x)} navigate={navigate} courseId={courseId}/></div></div>}
function Fees({data,navigate,states}){const f=data.fee_summary||{},registered=f.cricos_registered||[],current=f.provider_current||[];return <Section title="Fees" help="Registered CRICOS course-cost facts and Provider-current tuition are separate facts. Layer 2 never replaces the registered CRICOS values."><div className="m-semantic-grid"><div><small style={{display:'flex',alignItems:'center',gap:6}}>Registered CRICOS course cost <LayerBadge layer={1}/></small><strong>{registered.length}</strong><div className="m-record-list">{registered.length?registered.map((x,i)=><FeeRecord key={x.id||i} x={x} registered navigate={navigate} courseId={data.id}/>):<span>—</span>}</div></div><div><small style={{display:'flex',alignItems:'center',gap:6,flexWrap:'wrap'}}>Current Provider tuition <StateTrail state={states.get('provider_current_tuition')}/></small><strong>{current.length}</strong><div className="m-record-list">{current.length?current.map((x,i)=><FeeRecord key={x.id||i} x={x} navigate={navigate} courseId={data.id}/>):<span>—</span>}</div></div></div></Section>}
function EntryRequirements({data,states}){const intakes=data.intakes||[],english=data.english||[];const intakeValue=intakes.length?intakes.map(x=>[x.label,x.year].filter(Boolean).join(' ')).join(', '):null;const englishText=english.length?english.map(x=>{const test=x.test_name||x.test_code||'English test';const score=x.overall_score!=null?`Overall score ${x.overall_score}`:null;const confidence=x.confidence!=null?`Confidence ${x.confidence}`:null;return [test,score,confidence].filter(Boolean).join(' · ')}).join('; '):null;return <Section title="Intakes & English"><div className="m-kv-list"><Row label="Intakes" value={intakeValue} state={states.get('intakes')}/><Row label="English requirement" value={englishText} state={states.get('english_requirement')}/></div></Section>}
function Campuses({rows,state}){return <Section title="Campuses"><StateTrail state={state}/><div className="m-record-list">{rows?.length?rows.map((x,i)=><div className="m-record" key={x.id||i}><strong>{x.name||x.campus_name||'Campus'}</strong><span>{[x.city,x.subdivision_name||x.state,x.postcode].filter(Boolean).join(' · ')}</span></div>):<span>—</span>}</div></Section>}
function Regulatory({data,navigate,state}){const rows=data.regulatory_facts||[];return <Section title="Regulatory facts" help="Authoritative Layer 1 facts require source correction when missing; Layer 4 cannot overwrite them."><StateTrail state={state}/><div className="m-record-list">{rows.length?rows.map((x,i)=>{const parts=[];if(x.course_language)parts.push(`Language: ${x.course_language}`);if(x.work_component!=null)parts.push(`Work component: ${bool(x.work_component)}`);if(x.work_component_total_hours!=null)parts.push(`Work hours: ${Number(x.work_component_total_hours).toLocaleString()}`);return <div className="m-record" key={x.evidence_id||i}><strong>{String(x.scheme||'CRICOS').toUpperCase()} registration</strong><span>{parts.join(' · ')||'Current regulatory registration'}</span><EvidenceButton id={evidenceIdOf(x)} navigate={navigate} courseId={data.id}/></div>}):<span>—</span>}</div></Section>}
function Evidence({rows,navigate,courseId}){if(!rows?.length)return null;return <Section title="Evidence" help="Open an artifact to review source, capture, hash and lineage."><div className="m-record-list">{rows.map((x,i)=><button key={x.id||i} className="m-record" style={{textAlign:'left',width:'100%',cursor:'pointer'}} onClick={()=>x.id&&navigate?.('Evidence',{evidence_id:x.id,return_course_id:courseId})}><strong>{x.evidence_type||x.type||'Evidence artifact'}</strong><span>{[x.captured_at?`Captured ${fmtDate(x.captured_at)}`:null,x.source_url||null,x.content_hash?`SHA ${String(x.content_hash).slice(0,12)}…`:null].filter(Boolean).join(' · ')}</span><span style={{display:'inline-flex',alignItems:'center',gap:4,fontWeight:700,color:'#4f46e5'}}><BookOpen size={11}/> Open Evidence</span></button>)}</div></Section>}
function OptionalList({title,rows,state}){return <Section title={title}><StateTrail state={state}/><div className="m-record-list">{rows?.length?rows.map((x,i)=><div className="m-record" key={x.id||i}><strong>{x.name||x.title||x.code||title.slice(0,-1)}</strong><span>{[x.type,x.relationship_type,x.description].filter(Boolean).join(' · ')}</span></div>):<span>—</span>}</div></Section>}
function safeCount(v){if(v==null)return null;if(typeof v==='number'||typeof v==='string')return String(v);if(typeof v==='object'){if(typeof v.fields==='number')return String(v.fields);if(typeof v.count==='number')return String(v.count);if(typeof v.records==='number')return String(v.records);return null}return null}
function OperationalState({data}){const s=data.state_summary||{};if(!Object.keys(s).length)return null;const search=safeCount(s.search??s.search_fields),canonical=safeCount(s.canonical??s.canonical_fields),ready=safeCount(s.admin_readiness??s.admin_readiness_fields),channels=safeCount(s.consumer_channels);return <Section title="Operational state" help="Completeness/readiness never publishes a Course automatically. Publication requires explicit governed approval/action."><div className="m-kv-list"><Row label="Publication" value={data.publication_status||'—'}/>{search!=null&&<Row label="Search projection" value={`${search} field${Number(search)===1?'':'s'}`}/>} {canonical!=null&&<Row label="Canonical coverage" value={`${canonical} field${Number(canonical)===1?'':'s'}`}/>} {ready!=null&&<Row label="Admin readiness" value={`${ready} field${Number(ready)===1?'':'s'}`}/>} {channels!=null&&<Row label="Consumer channels" value={`${channels} record${Number(channels)===1?'':'s'}`}/>} </div></Section>}
export default function CourseDetailPolish({data,navigate}){if(!data)return null;const states=stateMap(data.field_states);return <div className="m-drawer-body"><Overview data={data} states={states}/><Fees data={data} navigate={navigate} states={states}/><EntryRequirements data={data} states={states}/><Campuses rows={data.campuses} state={states.get('campuses')}/><OptionalList title="Academic options" rows={data.academic_options} state={states.get('academic_options')}/><OptionalList title="Categories" rows={data.categories} state={states.get('categories')}/><OptionalList title="Collections" rows={data.collections} state={states.get('collections')}/><Regulatory data={data} navigate={navigate} state={states.get('regulatory_facts')}/><Evidence rows={data.evidence} navigate={navigate} courseId={data.id}/><OperationalState data={data}/></div>}
