# CourseFinder v2.15.75 Protected-Baseline Hardening Recovery & Re-apply Runbook

**Date:** 2026-09-10  
**Purpose:** Re-apply the proven architectural/tooling/security hardening without overwriting the accepted v2.15.75 bug fixes, UI behaviour, ranking lifecycle corrections, or release-currentness state.  
**Supersedes for execution:** `2026-09-10-v2.15.74-hardening-recovery-reapply-runbook.md`  
**Historical reference retained:** the v2.15.74 runbook remains valid as the detailed hardening lineage and must not be deleted.  

---

## 1. Golden rule

The immutable backup `backup/2.15.74_8-9-2026_10AM` is now a **forensic/recovery source**, not the final product baseline.

The target product baseline is **PIM Admin v2.15.75**.

Therefore:

1. preserve all accepted v2.15.74 UI/runtime behaviour that remains present in v2.15.75;
2. preserve all accepted v2.15.75 ranking, lifecycle, release-note and UI-currentness corrections;
3. re-apply only the missing hardening outcomes from the proven PR lineage;
4. never reset a v2.15.75 file back to a v2.15.74 copy merely because the hardening patch was originally authored against v2.15.74;
5. resolve conflicts by preserving the newer accepted functional/UI behaviour and adapting the hardening around it;
6. do not repeat the PR #52/#53 UAT folder restructure.

---

## 2. Protected v2.15.75 outcomes — MUST NOT BE OVERWRITTEN

### 2.1 QS Sources & Imports workflow

PR #59 changed the governed ranking import read surface so Administration → Sources & Imports presents **one visible active workflow row per ranking system + edition** while preserving all underlying source revisions and Evidence.

Protected implementation:

- `supabase/migrations/20260909131500_cf_ranking_import_history_one_row_per_edition.sql`
- `security.admin_ranking_imports_read(jsonb)` groups to latest visible revision by `(system_id, edition_year)`;
- `logical_revision_count` remains available so retained source revisions are still auditable;
- historical Evidence/manual import records are not destructively deleted.

Do not restore an older dispatcher/read function that returns every revision as a duplicate edition row.

### 2.2 QS 2024/2025 lifecycle cleanup

Protected runtime/data outcome:

- QS 2024 and 2025 each have one active Accepted edition;
- older duplicate active revisions were lifecycle-retired/superseded;
- Evidence and historical observations remain retained for audit;
- accepted editions were checked for duplicate institution observations.

Do not re-activate superseded revisions and do not destructively purge their Evidence/history.

### 2.3 QS 2026/2027 acquisition contract

PR #59 updated:

`supabase/functions/ranking-qs-url-import/index.ts`

Protected behaviour:

- Edge function version advanced to `ranking-qs-url-import-v1.1.0`;
- the governed static-indicator completeness contract includes 2026 and 2027;
- supported direct-publisher payloads for those years must not be forced into Parse.bot fallback merely because the old completeness map stopped at 2025.

Do not replace this function with the pre-v1.1.0 implementation.

### 2.4 Visible PIM Admin version and release-currentness UI

PR #61 published v2.15.75 to the maintained release-currentness surface.

Protected files/behaviour:

- `src/release-currentness-entry.js` must remain `VERSION='2.15.75'` unless a later authorised release supersedes it;
- the maintained accessible version-pill/release-dialog model remains the UI authority;
- v2.15.75 title remains the QS ranking duplicate cleanup / 2026–2027 acquisition correction;
- the release dialog includes the **Bug / UI fixes** subsection;
- historical release history remains preserved rather than replaced by a one-off popup;
- login/brand/document currentness reconciliation must continue to show v2.15.75.

Do not restore a v2.15.74 `release-currentness-entry.js` during hardening re-application.

### 2.5 v2.15.75 release governance files

Preserve:

- `docs/release-notes/v2.15.75.md`
- `docs/release-notes/README.md`
- `CHANGELOG.md`
- package version `0.1.1` or any later authorised package version
- `tests/uat/m245-release-currentness-ranking-fix-contract.spec.mjs`
- the v2.15.75 assertions added to `tests/uat/m245-release-dialog-history-contract.spec.mjs`

Hardening must adapt around these files. It must not revert package/release metadata to an older state.

### 2.6 Pending RLS security task remains separate

v2.15.75 explicitly records RLS remediation as pending security task #60.

Do not opportunistically enable RLS or change runtime policies while reconstructing architectural/tooling hardening unless the separate RLS task is explicitly authorised and fully validated.

---

## 3. Protected v2.15.74 UI behaviours that remain part of the v2.15.75 baseline

The move to v2.15.75 did not authorise removal of the previously accepted UI improvements. Preserve them unless current runtime truth proves a later authorised replacement.

Protected examples include:

- Provider Compare shows QS and THE as independent ranking sections;
- QS and THE have their own edition/year controls and independent multi-year behaviour;
- QILT retains its explicit year selector rather than inheriting the old shared ranking trend control;
- provider/university identity headers remain sticky/frozen while QILT, PRISMS and ranking comparison content scrolls;
- QS/THE dataset workspaces retain the accepted QILT/PRISMS-aligned presentation and dataset routing;
- the maintained release dialog/history and version-pill behaviour remain accessible and persistent after delayed authentication;
- breadcrumb/dashboard heading synchronisation fixes remain in place.

A hardening conflict with any of the above must be resolved by changing the hardening implementation, not by reverting the accepted UI behaviour.

---

## 4. Hardening lineage to preserve

Use the detailed v2.15.74 runbook for the full PR-by-PR history. The successful hardening lineage remains:

- PR #45 — architectural hardening refactor;
- PR #46 — active Course PIM runtime + governed `ui_course_detail` contract + safer PR-head handling;
- PR #47 — dedicated exact-revision Course PIM deployed UAT routing;
- PR #48 — CF-241 Evidence/Layer2 forward runtime reconciliation and missing derived runtime migration history;
- PR #50 — typed Supabase browser boundary and lean Dynamic PIM bridge.

Recreate those **semantics** on top of v2.15.75 rather than restoring older file snapshots wholesale.

---

## 5. UAT restructure boundary — still prohibited

Do not reapply:

- PR #52 — bulk movement of 39 suites from `tests/uat/` to `tests/retired-uat/`;
- PR #53 — deployed-UAT workflow rewiring around that active/retired split;
- PR #55 — discarded UAT authentication/sharding experiment.

PR #54 contains useful guardrails but must not be cherry-picked wholesale because it was developed through/around the restructured UAT surface.

### Non-negotiable UAT rule

During hardening recovery:

- do not move existing `tests/uat/**` files;
- do not rename existing test files;
- do not combine suites;
- do not weaken assertions to obtain green CI;
- optimise only through Playwright config, explicit workflow selection, caching, orchestration or separately approved wrappers;
- preserve historical/state-changing tests in place unless an explicit governed retirement is separately approved after dependency mapping.

---

## 6. Conflict-resolution rules for the new implementation

For every hardening file that also changed in v2.15.75 or in accepted UI work:

1. inspect the current v2.15.75 file first;
2. inspect the original hardening PR patch;
3. identify the semantic hardening requirement;
4. port only that requirement into the current file;
5. preserve newer UI/data/release behaviour;
6. add/adjust tests without deleting current accepted regression coverage;
7. request Codex review;
8. run targeted → bounded → nominated acceptance.

Never use `git checkout <old-commit> -- <current-file>` on a protected v2.15.75 file unless the entire resulting diff has been reviewed and proves no accepted behaviour is lost.

---

## 7. Files requiring special merge care

The following are high-risk because they are touched by hardening and/or protected UI/release behaviour:

- `package.json`
- `CHANGELOG.md`
- `src/release-currentness-entry.js`
- `src/mature-main.jsx`
- `src/Layer4Intervention.jsx`
- `src/pim-version-entry.js`
- `src/lib/supabase.js` / `src/lib/supabase.ts`
- `vite.config.js`
- `playwright.config.mjs`
- `.github/workflows/*.yml`
- `tests/uat/**`
- `supabase/functions/ranking-qs-url-import/index.ts`
- `supabase/migrations/20260909131500_cf_ranking_import_history_one_row_per_edition.sql`
- Course PIM / CF-241 migrations from PR #46/#48.

Treat these as three-way semantic merges, not file replacements.

---

## 8. New Chat required preflight

Before editing anything, the new chat must report:

1. current `main` SHA and visible PIM Admin version;
2. current Cloudflare deployed SHA/version;
3. current package version;
4. confirmation that the v2.15.75 release note and Bug/UI fixes are present;
5. confirmation that QS Sources & Imports is one active row per system+edition;
6. confirmation that QS 2024/2025 lifecycle state is still one active Accepted edition each with historical revisions retained;
7. confirmation that the QS URL import function contains the 2026/2027 completeness contract and v1.1.0-or-later behaviour;
8. current inventory of `tests/uat/**` and whether any files have already been moved/renamed;
9. which PR #45/#46/#47/#48/#50 hardening outcomes are already present versus missing;
10. exact files proposed for Phase 1 and which protected v2.15.75 behaviours each file must preserve.

No hardening write should occur until this preflight is complete.

---

## 9. Acceptance definition

The recovery is successful only when all of the following are simultaneously true:

**Product/UI baseline**

- v2.15.75 visible currentness retained;
- accepted 2.15.74 UI fixes retained;
- QS duplicate-edition presentation remains corrected;
- QS lifecycle history remains preserved;
- 2026/2027 direct-publisher acquisition contract remains corrected;
- release dialog/history and Bug/UI fixes remain intact.

**Hardening**

- TypeScript/ESLint/Vitest guardrails restored;
- typed governed Supabase/admin_read browser boundary restored;
- Dynamic PIM governed contract restored;
- exact deployed-revision verification restored;
- selected DevOps/security guardrails restored without UAT relocation;
- CF-241 replay/runtime safety restored if not already present.

**UAT**

- original/current governed UAT corpus remains at its accepted paths;
- no broad retirement-folder restructure;
- targeted → bounded → nominated acceptance passes;
- no valid Codex P1/P2 remains unresolved.

**Security/governance**

- no direct browser-table access introduced;
- no incomplete RLS remediation smuggled into the recovery;
- no Production change without separate explicit authorisation.

---

## 10. Short instruction for a new Chat

Treat v2.15.75 as the protected product baseline. Use v2.15.74 backup/history only to reconstruct missing hardening semantics. Never overwrite newer QS fixes, lifecycle state, release-currentness/UI fixes, package/release metadata, or accepted 2.15.74 UI behaviour with older files. Do not repeat the PR #52/#53 UAT restructure. Port hardening file-by-file, preserving current functionality, and validate each phase with Codex plus targeted → bounded → nominated UAT.
